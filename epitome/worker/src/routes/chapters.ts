// Replaced by pages.ts — kept as empty stub so the file isn't a dangling import.
import { Hono } from 'hono';
import type { Bindings } from '../types';
export default new Hono<{ Bindings: Bindings }>();
