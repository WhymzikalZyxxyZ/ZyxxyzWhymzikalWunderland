{
    "name": "anonymail-worker",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "test": "vitest run",
        "test:coverage": "vitest run --coverage"
    },
    "dependencies": {
        "postal-mime": "^2.3.7",
        "qrcode": "^1.5.4"
    },
    "devDependencies": {
        "vitest": "^4.1.8",
        "wrangler": "^4.110.0"
    }
}
