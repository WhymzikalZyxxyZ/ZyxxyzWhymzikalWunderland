{
    "name": "anonymail-worker",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy"
    },
    "dependencies": {
        "postal-mime": "^2.3.7",
        "qrcode": "^1.5.4"
    },
    "devDependencies": {
        "wrangler": "^4.96.0"
    }
}
