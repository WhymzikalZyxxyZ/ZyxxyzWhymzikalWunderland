name: zyxxyz_flutter
description: Zyxxyz Wunderland chess app — Flutter multiplatform.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  chess_engine:
    path: ../shared

flutter:
  uses-material-design: true
