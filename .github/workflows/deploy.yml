name: Deploy to GitHub Pages + Cloudflare + Fly.io

# Fires when main is updated (typically by promote.yml after CI passes).
# By the time code lands on main, the full CI matrix has already blessed it,
# so no redundant lint/test jobs are run here — deploy straight away.

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:

  # ── Deploy editor-service to Fly.io ──────────────────────────────────────────
  deploy-editor-service:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up flyctl
        uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Deploy editor-service to Fly.io
        run: flyctl deploy --remote-only
        working-directory: editor-service
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

  # ── Deploy Cloudflare Worker (Anonymail) ─────────────────────────────────────
  deploy-worker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Deploy Anonymail Worker to Cloudflare
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken:         ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId:        ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: anonymail/worker

  # ── Deploy Cloudflare Worker (The Locator) ────────────────────────────────────
  deploy-locator-worker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'

      - name: Build The Locator client
        run: |
          cd the-locator/client
          npm ci
          npm run build

      - name: Deploy The Locator Worker to Cloudflare
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken:         ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId:        ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: the-locator/worker
          secrets:          |
            MAPBOX_TOKEN
            CENSUS_API_KEY
        env:
          MAPBOX_TOKEN:      ${{ secrets.LOCATOR_MAPBOX_TOKEN }}
          CENSUS_API_KEY:    ${{ secrets.LOCATOR_CENSUS_API_KEY }}

  # ── Deploy Cloudflare Worker (ELINAL) ────────────────────────────────────────
  deploy-elinal-worker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'

      - name: Install ELINAL client dependencies
        run: cd elinal/client && npm ci

      - name: Build ELINAL client
        run: cd elinal/client && npm run build

      - name: Install ELINAL worker dependencies
        run: cd elinal/worker && npm ci

      - name: Test ELINAL worker
        run: cd elinal/worker && npm test

      - name: Deploy ELINAL Worker to Cloudflare
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken:         ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId:        ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: elinal/worker
          secrets:          |
            ADMIN_TOKEN
        env:
          ADMIN_TOKEN:      ${{ secrets.ELINAL_ADMIN_TOKEN }}

      # Migration runs after the worker deploy, so new worker code is live first.
      # Current migration uses IF NOT EXISTS — safe regardless of order.
      # CAUTION: If a future migration adds or renames columns, deploy the worker
      # AFTER the migration completes, not before, to avoid a schema mismatch window.
      - name: Run ELINAL D1 migration
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken:         ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId:        ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: elinal/worker
          command:          d1 execute elinal-db --remote --file=migrations/0001_initial.sql

  # ── Deploy Cloudflare Worker (Epitome) ───────────────────────────────────────
  deploy-epitome-worker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'

      - name: Build Epitome client
        run: |
          cd epitome/client
          npm ci
          npm test
          npm run build

      - name: Install Epitome worker dependencies
        run: cd epitome/worker && npm ci

      - name: Test Epitome worker
        run: cd epitome/worker && npm test

      - name: Deploy Epitome Worker to Cloudflare
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken:         ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId:        ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: epitome/worker

      - name: Run Epitome D1 migration 0001
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken:         ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId:        ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: epitome/worker
          command:          d1 execute epitome --remote --file=drizzle/0001_initial.sql

      - name: Run Epitome D1 migration 0002 (chapters + mainCoverKey)
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken:         ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId:        ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: epitome/worker
          command:          d1 execute epitome --remote --file=drizzle/0002_chapters.sql

  # ── Deploy static site to GitHub Pages ───────────────────────────────────────
  deploy:
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Generate Firebase config
        env:
          FIREBASE_API_KEY:             ${{ secrets.FIREBASE_API_KEY }}
          FIREBASE_AUTH_DOMAIN:         ${{ secrets.FIREBASE_AUTH_DOMAIN }}
          FIREBASE_DATABASE_URL:        ${{ secrets.FIREBASE_DATABASE_URL }}
          FIREBASE_PROJECT_ID:          ${{ secrets.FIREBASE_PROJECT_ID }}
          FIREBASE_STORAGE_BUCKET:      ${{ secrets.FIREBASE_STORAGE_BUCKET }}
          FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}
          FIREBASE_APP_ID:              ${{ secrets.FIREBASE_APP_ID }}
        run: |
          node -e "
          const cfg = {
              apiKey:            process.env.FIREBASE_API_KEY,
              authDomain:        process.env.FIREBASE_AUTH_DOMAIN,
              databaseURL:       process.env.FIREBASE_DATABASE_URL,
              projectId:         process.env.FIREBASE_PROJECT_ID,
              storageBucket:     process.env.FIREBASE_STORAGE_BUCKET,
              messagingSenderId: process.env.FIREBASE_MESSAGING_SENDER_ID,
              appId:             process.env.FIREBASE_APP_ID,
          };
          const out = 'const FIREBASE_CONFIG = ' + JSON.stringify(cfg, null, 4) + ';\n' +
              'const FIREBASE_READY = FIREBASE_CONFIG.apiKey !== \"YOUR_API_KEY\";\n' +
              'if (FIREBASE_READY && !firebase.apps.length) { firebase.initializeApp(FIREBASE_CONFIG); }\n';
          require('fs').writeFileSync('js/config/firebase-config.js', out);
          "

      - name: Minify assets
        run: |
          docker run --rm \
            -v "$PWD:/app" -w /app \
            node:22-alpine \
            sh -c "npm ci && node scripts/minify.js"

      - name: Configure Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  # ── Smoke tests ───────────────────────────────────────────────────────────────
  # Runs after all deploy jobs complete. Gives workers ~20s to propagate before
  # hitting the live endpoints.
  smoke-test:
    name: Smoke test deployed services
    runs-on: ubuntu-latest
    needs: [deploy, deploy-worker, deploy-locator-worker, deploy-editor-service, deploy-elinal-worker, deploy-epitome-worker]
    steps:
      - name: Wait for propagation
        run: sleep 20

      - name: Smoke test — static site (200 OK)
        run: |
          STATUS=$(curl -fsS -o /dev/null -w "%{http_code}" https://zyxwonderland.xyz/)
          echo "Static site: $STATUS"
          [ "$STATUS" = "200" ] || (echo "Expected 200, got $STATUS" && exit 1)

      - name: Smoke test — Locator Worker health endpoint
        run: |
          BODY=$(curl -fsS https://locator.zyxwonderland.xyz/api/health)
          echo "Locator health: $BODY"
          echo "$BODY" | python3 -c "
          import sys, json
          d = json.load(sys.stdin)
          assert d.get('status') == 'ok', f'unexpected health response: {d}'
          print('OK')
          "

      - name: Smoke test — ELINAL Worker health endpoint
        run: |
          BODY=$(curl -fsS https://elinal.zyxwonderland.xyz/api/health)
          echo "ELINAL health: $BODY"
          echo "$BODY" | python3 -c "
          import sys, json
          d = json.load(sys.stdin)
          assert 'status' in d, f'missing status field: {d}'
          assert 'checks' in d, f'missing checks field: {d}'
          print('OK:', d.get('status'))
          "

      - name: Smoke test — Epitome Worker health endpoint
        run: |
          BODY=$(curl -fsS https://epitome.zyxwonderland.xyz/api/health)
          echo "Epitome health: $BODY"
          echo "$BODY" | python3 -c "
          import sys, json
          d = json.load(sys.stdin)
          assert d.get('ok') == True, f'unexpected health response: {d}'
          print('OK')
          "
